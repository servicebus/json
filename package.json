{
  "name": "@servicebus/json",
  "version": "0.0.0-development",
  "description": "json middleware for servicebus",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier-standard 'index.js'"
  },
  "lint-staged": {
    "linters": {
      "**/*.js": [
        "prettier-standard",
        "git add"
      ]
    }
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged && npm run test"
    }
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/servicebus/json.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/servicebus/json/issues"
  },
  "homepage": "https://github.com/servicebus/json#readme",
  "devDependencies": {
    "husky": "^1.3.1",
    "jest": "^24.7.0",
    "lint-staged": "^8.1.5",
    "prettier-standard": "^9.1.1"
  }
}
